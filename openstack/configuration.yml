---
  - hosts: jenkins
    become: true
    roles:
      - jenkins_owner
      - geerlingguy.java

  - hosts: jenkins_master
    become: yes
    vars:
      jenkins_hostname: 10.0.10.20
      jenkins_http_port: 8000
    roles:
      - geerlingguy.jenkins

  - hosts: jenkins_master
    roles:
      - jenkins_owner
      - jenkins_master_config